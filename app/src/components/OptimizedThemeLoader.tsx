"use client";

// Theme loader component is no longer needed since we've simplified to a pure black & white theme
// All theming is now handled via CSS variables in globals.css

export function OptimizedThemeLoader() {
  // Clean black & white theme is applied via globals.css
  // No dynamic theme loading needed
  return null;
}
