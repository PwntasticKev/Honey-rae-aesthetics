{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--stdio",
        "--browser=chromium",
        "--headless"
      ],
      "transport": "stdio",
      "timeoutMs": 120000
    }
  }
}
